|===
|Path|Type|Description

|`+[].rankingScoreId+`
|`+Number+`
|Ranking score identifier.

|`+[].collectedItemId+`
|`+Number+`
|Collected item identifier.

|`+[].title+`
|`+String+`
|Collected item title.

|`+[].summary+`
|`+String+`
|Collected item summary used for topic-specific briefing generation.

|`+[].canonicalUrl+`
|`+String+`
|Canonical URL of the collected item.

|`+[].backendRelevanceScore+`
|`+Number+`
|Score for backend career relevance.

|`+[].learningValueScore+`
|`+Number+`
|Score for learning value.

|`+[].implementationValueScore+`
|`+Number+`
|Score for hands-on implementation value.

|`+[].totalScore+`
|`+Number+`
|Sum of ranking score dimensions.

|`+[].reasons+`
|`+String+`
|Human-readable reasons produced by ranking rules.

|===